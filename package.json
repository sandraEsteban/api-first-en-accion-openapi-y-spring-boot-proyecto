{
  "name": "acme-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o dist/bundle.yaml",
    "test": "redocly lint"
  },
  "devDependencies": {
    "@redocly/cli": "^2.26.0"
  }
}
