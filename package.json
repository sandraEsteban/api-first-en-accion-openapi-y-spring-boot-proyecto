{
  "name": "acme-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o dist/bundle.yaml",
    "test": "redocly lint",
    "build:public": "redocly bundle public@v2 -o dist/public.yaml",
    "build:internal": "redocly bundle internal@v1 -o dist/internal.yaml",
    "start:api": "reunite start dist/public.yaml"
  },
  "devDependencies": {
    "@redocly/cli": "^2.31.2",
    "@stoplight/spectral-cli": "^6.15.0"
  }
}
